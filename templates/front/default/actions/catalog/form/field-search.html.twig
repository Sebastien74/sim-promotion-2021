{% trans_default_domain 'front_default' %}

{% block _search_products_text_widget %}
    <div class="input-group mb-3">
        <input type="text" id="{{ id }}" name="{{ full_name }}" placeholder="{{ attr.placeholder }}" class="form-control" value="{{ value }}">
        <div id="search-product-text-submit" class="input-group-append cursor">
            <span class="input-group-text">{{ "fal fa-search"|fontawesome(null, 14) }}</span>
        </div>
    </div>
{% endblock %}