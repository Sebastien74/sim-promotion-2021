{% trans_default_domain 'admin' %}

{% if entity.actions|length > 0 %}
    <ul class="infos-list reset text-italic">
        {% for action in entity.actions %}
            <li><small>- {{ action.adminName }}</small></li>
        {% endfor %}
    </ul>
{% endif %}