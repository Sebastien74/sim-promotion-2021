{% trans_default_domain 'admin' %}

<div id="actives-sessions-card" class="card bg-info-darken h-100">
    <div class="card-body">
        <h4 class="card-title mb-3">{{ "Utilisateurs actifs actuellement"|trans }}</h4>
        <span class="counter">{{ actives|length }}</span>
        <h6 class="mt-4">{{ "Pages vues par minute"|trans }}</h6>
        <div class="content d-inline-block mb-4"></div>
        <h6 class="mt-4">
            <div class="row">
                <div class="col-md-8">{{ "Principales pages actives"|trans }}</div>
                <div class="col-md-4 text-right">{{ "Utilisateurs actifs"|trans }}</div>
            </div>
        </h6>
        <p class="no-data mb-0">{{ "Aucune donnée disponible pour cette vue."|trans }}</p>
    </div>
</div>